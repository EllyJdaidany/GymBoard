SUPABASE_URL=
SUPABASE_KEY=
OPL_API_BASE_URL=https://closepowerlifting.com
# Browser origins allowed to call the API (comma-separated, no trailing slash)
CORS_ORIGINS=http://localhost:5173,https://gym-board-weld.vercel.app
# Optional single frontend URL (merged into allowed origins)
# FRONTEND_URL=https://gym-board-weld.vercel.app
# Optional regex for preview deploys; default allows all *.vercel.app over https
# CORS_ORIGIN_REGEX=https://([a-z0-9-]+\.)*vercel\.app
